[tool.poetry]
name = "kurs-2-kristi"
version = "0.1.0"
description = "Job search application with hh.ru API integration"
authors = ["Your Name <you@example.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"
requests = "^2.31.0"
pytest = "^8.3.5"
pytest-cov = "^6.1.1"

[tool.poetry.group.dev.dependencies]
flake8 = "^7.1.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"